describe("handleFetchPageTitle", () => {
  // TODO: add tests
  // eslint-disable-next-line jest/no-commented-out-tests
  // it("ページタイトル取得リクエストを受けて変換できる", async () => {
  // const req = new FetchPageTitleRequest();
  // req.setXxx(xxx)
  // const ctx = new Map<string, unknown>();
  // const reply = await handleFetchPageTitle(req, ctx);
  // expect(reply.getXxx()).toBe(...);
  // });
});

// テストがひとつもないと jest に怒られるので用意してあるダミーのテストです
test("dummy", () => {
  expect(1).toBe(1);
});
